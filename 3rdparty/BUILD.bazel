load("@rules_rust//crate_universe:defs.bzl", "crate", "crates_vendor")

exports_files(
    ["BUILD.freestnd_c_hdrs.bazel"],
    visibility = ["//visibility:public"],
)

crates_vendor(
    name = "crates_vendor",
    annotations = {
    },
    cargo_lockfile = "Cargo.Bazel.lock",
    generate_build_scripts = False,
    mode = "remote",
    packages = {
        "bitflags": crate.spec(
            version = "=2.11.0",
        ),
        "limine": crate.spec(
            version = "=0.5.0",
        ),
        "eclipse_framebuffer": crate.spec(
            version = "=0.1.0",
        ),
        "nom": crate.spec(
            default_features = False,
            features = ["alloc"],
            version = "8.0.0",
        ),
        "memchr": crate.spec(
            default_features = False,
            features = ["alloc"],
            version = "2.7.0",
        ),
        "thiserror-no-std": crate.spec(
            features = [],
            version = "2.0",
        ),
    },
    repository_name = "crate_index",
    supported_platform_triples = [
        "x86_64-unknown-none",
        "aarch64-unknown-uefi",  # Apparently, Bazel's rules_rust doesn't support the aarch64-unknown-none triple
        # Note that the host's toolchain is required for rules_rust to fetch the crates' features
        "aarch64-apple-darwin",
        "x86_64-unknown-linux-gnu",
    ],
    tags = ["manual"],
)
